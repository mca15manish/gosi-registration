<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>QR Code Result</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      @font-face {
        font-family: 'PingAR LT';
        src: url('/fonts/PingAR+LT-Regular.otf') format('opentype');
        font-weight: 400;
        font-style: normal;
      }
      @font-face {
        font-family: 'PingAR LT';
        src: url('/fonts/PingAR+LT-Medium.otf') format('opentype');
        font-weight: 500;
        font-style: normal;
      }
      @font-face {
        font-family: 'PingAR LT';
        src: url('/fonts/PingAR+LT-Bold.otf') format('opentype');
        font-weight: 700;
        font-style: normal;
      }

      body {
        font-family: 'PingAR LT', Arial, sans-serif;
        background-color: #00152f;
        background-image: url("/bg.png");
        background-repeat: no-repeat;
        background-position: center top;
        background-size: contain;
        color: #ffffff;
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        flex-direction: column;
        overflow: hidden;
        margin: 0;
      }

      .content-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        min-height: 80vh;
        margin-top: 2vh;
      }

      h2 {
        color: #ffffff;
        font-weight: 700;
        margin-bottom: 0.25rem;
        font-size: 1.6rem;
        letter-spacing: 0.2px;
      }

      h4 {
        color: #ffffff;
        font-weight: 500;
        font-size: 1.05rem;
        margin-top: 0;
        margin-bottom: 0.4rem; /* tighter spacing */
      }

      /* 👇 Added User ID style */
      .user-id {
        font-size: 0.95rem;
        color: #cccccc;
        margin-bottom: 1rem;
      }

      img#qrImage {
        width: 210px;
        height: 210px;
        border: 2px solid #ffffff;
        border-radius: 10px;
        margin: 12px 0 24px 0;
      }

      .btn-custom {
        background-color: #009000;
        color: white;
        font-weight: 600;
        border: none;
        border-radius: 6px;
        padding: 0.6rem 0;
        font-size: 0.95rem;
        width: 100%;
        max-width: 280px;
        letter-spacing: 0.3px;
        transition: all 0.3s ease;
      }

      .btn-custom:hover {
        background-color: #00b000;
        transform: scale(1.02);
      }

      @media (max-height: 700px) {
        .content-wrapper {
          min-height: 70vh;
          margin-top: 0;
        }
        img#qrImage {
          width: 190px;
          height: 190px;
        }
      }

      @media (max-width: 600px) {
        body {
          background-size: contain;
          background-position: top center;
        }
        .content-wrapper {
          min-height: 75vh;
        }
        h2 {
          font-size: 1.4rem;
        }
        h4 {
          font-size: 1rem;
          margin-bottom: 0.3rem;
        }
        .user-id {
          font-size: 0.9rem;
          margin-bottom: 0.8rem;
        }
      }
    </style>
  </head>

  <body>
    <div class="content-wrapper">
      <h2>Registration Successful!</h2>
      <h4><%= firstName %> <%= lastName %></h4>

      <!-- ✅ Display the unique ID -->
      <p class="user-id">User ID: <%= uniqueID %></p>

      <img id="qrImage" src="<%= qrCodeData %>" alt="QR Code" />

      <a href="/" class="btn btn-custom mt-3">Register New User</a>
    </div>
  </body>
</html>
